{
  "name": "typescriptgb",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "rimraf public/scripts && tsc",
    "build:dev": "rimraf public/scripts && tsc -p tsconfig-dev.json",
    "start": "http-server -p 3000",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "eslint src --ext .js --ext .ts",
    "lint-fix": "eslint src --ext .js --ext .ts --fix"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "http-server": "^14.1.1",
    "typescript": "^4.9.4"
  },
  "dependencies": {
    "@typescript-eslint/eslint-plugin": "^5.48.1",
    "@typescript-eslint/parser": "^5.48.1",
    "eslint": "^8.31.0",
    "rimraf": "^4.0.4"
  }
}